#include <stdio.h>
int main(){
    int age,,citizen;
    scanf("%d %d", &a,&b);
    if(age>=18 && citizen == 1){
        printf("Eligible");
    }else {
        printf("Not Eligibe");
    }
    return 0;
}